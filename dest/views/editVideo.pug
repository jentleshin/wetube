extends layouts/main.pug

block content
    .form-container
        form(action=routes.editVideo({fullRoute: true, id: id}), method="post")
            input(type="text", placeHolder="Title", name="title")
            textarea(placeHolder="Descrition", name="description")
            input(type="submit", value="Update Video")
    a.form-container__link-delete(href=routes.deleteVideo({fullRoute: true})) Delete Video
    //should change href